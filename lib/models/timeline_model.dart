class TimelineModel {
  String name;
  String profileImageURL;
  String imageURL;
  String detail;
  String time;

  TimelineModel(this.name, this.profileImageURL, this.imageURL, this.detail, this.time);
}
